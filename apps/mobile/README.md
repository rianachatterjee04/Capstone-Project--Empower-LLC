# Foundry People Mobile (scaffold)

Expo + React Native starter.
Next steps:
- Supabase auth (magic link)
- Onboarding + reporting screens
- Push notifications for escalations

Run:
```bash
cd apps/mobile
npm install
npx expo start
```
